# Express API — الإنتاج يستخدم Supabase مباشرة (VITE_USE_SUPABASE=1)
server-api/

# باكند محلي / OAuth — لا يُنشر على Vercel
backend/

# Edge functions تُنشر يدوياً على Supabase
supabase/

# نسخة Hostinger المحلية قبل البناء
hostinger-dist/
